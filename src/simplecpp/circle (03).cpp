/**	Name -- Saloni chipade
        Division -- B
        Roll No. -- 12
        Batch -- B1
        Program -- To Draw A cricle */

#include <simplecpp>

main_program {
  turtleSim();

  repeat(360) {
    forward(1);
    left(1);
  }

  wait(20);
}
