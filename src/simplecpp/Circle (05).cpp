/**	Name -- Salas Dongaonkar
        Division -- B
        Roll No. -- 16
        Batch -- B1
        Program -- To Draw A Circle Using Turtle Simulator */

#include <simplecpp>

main_program {
  turtleSim();

  repeat(360) {
    forward(1);
    left(1);
  }

  wait(10);
}
