/**	Name -- Saloni chipade
        Division -- B
        Roll No. -- 12
        Batch -- B1
        Program -- To Draw A square */

#include <simplecpp>

main_program {
  turtleSim();

  repeat(4) {
    forward(100);
    left(90);
    wait(2);
  }

  wait(10);
}
