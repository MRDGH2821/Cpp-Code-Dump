/**	Name -- Sharwari Daware
        Division -- B
        Roll No. -- 14
        Batch -- B1 */
#include <simplecpp>

main_program {
  turtleSim();

  repeat(360) {
    forward(1);
    left(1);
  }

  repeat(360) {
    forward(1);
    right(1);
  }

  wait(3);
}
