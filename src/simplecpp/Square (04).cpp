/**	Name -- Salas Dongaonkar
        Division -- B
        Roll No. -- 16
        Batch -- B1
        Program -- To Draw A Square Using Turtle Simulator */

#include <simplecpp>

main_program {
  turtleSim();

  repeat(4) {
    forward(100);
    left(90);
    wait(2);
  }

  wait(10);
}
