/**	Name -- Saloni chipade
        Division -- B
        Roll No. -- 12
        Batch -- B1
        Program -- To Draw A Hexagon Using Turtle Simulator */

#include <simplecpp>

main_program {
  turtleSim();

  repeat(6) {
    forward(70);
    left(60);
  }

  wait(10);
}
