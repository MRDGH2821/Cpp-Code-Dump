/**	Name -- Sharwari Daware
        Division -- B
        Roll No. -- 14
        Batch -- B1 */
#include <simplecpp>

main_program {
  turtleSim();

  repeat(4) {
    forward(100);
    left(90);
    wait(2);
  }

  wait(10);
}
