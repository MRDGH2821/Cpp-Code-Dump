/**	Name -- Sharwari Daware
        Division -- B
        Roll No. -- 14
        Batch -- B1 */
#include <simplecpp>
main_program {
  turtleSim();
  repeat(360) {
    forward(3);
    left(1);
  }

  wait(5);
}
